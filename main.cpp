#include <iostream>
#// include "image.cu"
// #include "blur.hpp"

int main(){

    //Image image("cameraman.npy");
    // Image output(image.nrows, image.ncols);
    // // Image blurred_image(nrows, ncols);


    // int nrows = 31;
    // int ncols = 31;
    // float sigma_col = 0.01;
    // float sigma_row = 0.01;
    // GaussianBlur op(sigma_row, sigma_col, 
    //                 nrows, ncols);   
    // 
    // // op.psf->save_npy("t.npy");

    // op.forward(output, image);
    // output.save_npy("t.npy");

    return 0;
}
